<?php

    $numbers = explode(" ", readline());
    $position = count($numbers);
    $sum = 0;

    for ($i = 0; $i < $position; $i++) {
        if ($numbers[$i] % 2 == 0) {
            $sum += $numbers[$i];
        }
    }
    echo $sum;
