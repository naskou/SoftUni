<?php

$number1 = floatval(readline());
$number2 = floatval(readline());
$esp = 0.000001;
$diff = floatval(abs($number1 - $number2));

if ($diff == $esp) {
    echo "False";
} else if ($diff > $esp) {
    echo "False";
} else if ($diff < $esp) {
    echo "True";
}
