<?php

    $input = readline();

    while (true) {
        if($input=="END"){
            break;
        }
        if ($input == strrev($input)) {
            echo "true" . PHP_EOL;
        } else {
            echo "false" . PHP_EOL;
        }
        $input=readline();
    }
